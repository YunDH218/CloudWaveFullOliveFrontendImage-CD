apiVersion: apps/v1
kind: Deployment
metadata:
  name: eu-web-deploy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: eu-web
  template:
    metadata:
      labels:
        app: eu-web
    spec:
      containers:
        - name: eu-web-container
          image: 882157583740.dkr.ecr.eu-central-1.amazonaws.com/frontend-frankfurt:18a189d
          ports:
            - containerPort: 3000
          resources:
            limits:
              cpu: 500m
            requests:
              cpu: 200m
---
apiVersion: v1
kind: Service
metadata:
  name: eu-web-service
spec:
  selector:
    app: eu-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: eu-web-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: eu-web-deploy
  minReplicas: 1
  maxReplicas: 50
  targetCPUUtilizationPercentage: 50
